#ifndef UTILS_H
#define UTILS_H

/* Function prototypes */
void clearBuffer();
void removeNewLine(char* str, int len);
int containsSpace(char* str, int len);
int equalsStrings(const char* str1, const char* str2);

#endif
